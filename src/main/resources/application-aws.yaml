spring:
  application:
    name: metric-server

  datasource:
    url: jdbc:postgresql://database-metric.cdjnxpoznoqv.eu-central-1.rds.amazonaws.com:5432/postgres
    username: postgres
    password: Metric1234
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      # update: створює/оновлює таблиці автоматично (добре для розробки)
      # validate: лише перевіряє схему (рекомендовано для продакшну)
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

mqtt:
  client-id: metric-db
  host-name: a3qlrcavgmj00y-ats.iot.eu-central-1.amazonaws.com
  certificate-path: /certs/metricServer.cert.pem
  private-key-path: /certs/metricServer.private.key